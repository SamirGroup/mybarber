# Bakery ERP — Python dependencies
# Install with: pip install -r requirements.txt

# Core framework
Django==6.0.4

# Django async support
asgiref==3.11.1

# SQL parsing (used internally by Django)
sqlparse==0.5.5

# Timezone data (required on Windows)
tzdata==2026.1

# Translation UI (matches INSTALLED_APPS + /rosetta/ URL)
django-rosetta>=0.10.3

# Required for Django ImageField (hr.Employee.photo, production.Product.image)
Pillow
whitenoise
gunicorn
openpyxl>=3.1.5
